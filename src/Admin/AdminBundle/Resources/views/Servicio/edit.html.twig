{% extends "AdminAdminBundle::layout.html.twig" %}

{% block container %}
    <h1>Servicio edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('dashboard_servicio') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
