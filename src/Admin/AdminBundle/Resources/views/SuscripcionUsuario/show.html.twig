{% extends "AdminAdminBundle::layout.html.twig" %}

{% block container %}
    <h1>SuscripcionUsuario</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ entity.activo }}</td>
            </tr>
            <tr>
                <th>Tarifa</th>
                <td>{{ entity.tarifa }}</td>
            </tr>
            <tr>
                <th>Dias</th>
                <td>{{ entity.dias }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('dashboard_suscripcionusuario') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('dashboard_suscripcionusuario_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
