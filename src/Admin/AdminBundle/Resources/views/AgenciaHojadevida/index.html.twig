{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AgenciaHojadevida list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Estado</th>
                <th>Tags</th>
                <th>Categoria</th>
                <th>Activo</th>
                <th>Fecha</th>
                <th>Fechaupdate</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('agenciahojadevida_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.Estado }}</td>
                <td>{{ entity.Tags }}</td>
                <td>{{ entity.categoria }}</td>
                <td>{{ entity.Activo }}</td>
                <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.fechaupdate %}{{ entity.fechaupdate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('agenciahojadevida_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('agenciahojadevida_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('agenciahojadevida_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
