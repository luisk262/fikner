{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AgenciaHojadevida</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ entity.Estado }}</td>
            </tr>
            <tr>
                <th>Tags</th>
                <td>{{ entity.Tags }}</td>
            </tr>
            <tr>
                <th>Categoria</th>
                <td>{{ entity.categoria }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ entity.Activo }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ entity.fecha|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Fechaupdate</th>
                <td>{{ entity.fechaupdate|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('agenciahojadevida') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('agenciahojadevida_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
