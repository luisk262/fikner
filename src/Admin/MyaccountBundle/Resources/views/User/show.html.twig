{% extends 'AdminMyaccountBundle::layout.html.twig'%}
{% block container %}
   
    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Username</th>
                <td>{{ entity.username }}</td>
            </tr>
            <tr>
                 <th>Nombre</th>
                <td>{{ entity.nombre }}</td>
            </tr>
            <tr>
             <th>apellidos</th>
                <td>{{ entity.apellidos }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ entity.telefono }}</td>
            </tr>
            <tr>
                 <th>Email</th>
                <td>{{ entity.email }}</td>
            </tr>
            <tr>
                 <th>Fecha de nacimiento</th>
                 <td>{{ entity.fechaNaci|date('d-M-y') }}</td>
            </tr>
        </tbody>
    </table>
            <div class="col-lg-12">
                <div class="col-lg-4 col-lg-offset-4">
                <a href="{{ path('Myaccount_user_edit') }}" class="btn btn-danger btn-block">
                    Editar
                </a>
                    </div>
            </div>
                 
{% endblock %}
