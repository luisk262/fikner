<!DOCTYPE html>
<html lang="es">
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Fikner - Book {{id}}</title>
        <meta name="description" content="">
        {% if photos %}
            {% if entities[0].image == '' %}
                <link rel="icon" type="image/jpeg" href="{{ asset('upload/default/default.png')}}" />
            {% else %}
                <link rel="icon" type="image/jpeg" href="{{web_image('upload/Photos/' ~ entities[0].id ~'/' ~ entities[0].image).resize(100,100)}}" />
            {%endif%}
            {% if HojadevidaP[0].idHojadevida.id != null %}
                {% set number1 = HojadevidaP[0].idHojadevida.fechaNac|date("Y") %}
                {% set number2 = "now"|date("Y") %}
                {% set aux= ((number2 - number1)) %}
                <meta property="og:description" content="Edad:{{aux}} Estatura:{{HojadevidaP[0].idHojadevida.estatura}}Cm Cabello:{{HojadevidaP[0].idHojadevida.pelo}}  Piel:{{HojadevidaP[0].idHojadevida.piel}} Ojos:{{HojadevidaP[0].idHojadevida.ojos}} Peso:{{HojadevidaP[0].idHojadevida.peso}}Kg Camisa:{{HojadevidaP[0].idHojadevida.tallaCamisa}} Pantalon:{{HojadevidaP[0].idHojadevida.tallaPantalon}} Calzado:{{HojadevidaP[0].idHojadevida.tallaZapato}}">
            {% else %}
                <meta property="og:description" content="No existe este book en fikner.com">
            {%endif%}

        {%else%}
            <link rel="icon" type="image/jpeg" href="{{ asset('upload/default/default.png')}}" />
        {%endif%}
        <link rel="stylesheet" href="{{asset('/bundles/adminmyaccount/css/book/bootstrap.min.css')}}">
        <link rel="stylesheet" href="{{asset('/bundles/adminmyaccount/css/book/styles/css/fiknerStyles.min.css')}}" media="screen" title="no title" charset="utf-8">
        <link rel="stylesheet" href="{{asset('/bundles/adminmyaccount/css/book/animate.css')}}" media="screen" title="no title" charset="utf-8">
    </head>
    <body>
        <!-- Fixed navbar -->
        <nav class="navbar navbar-default navbar-fixed-top">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand" href="{{path('principal')}}"><img src="{{web_image('images/logo/fikner_logo_blanco.png').resize(90,30)}}" alt="" height="30px"></a>
                </div>

            </div>
        </nav>
        <section class="books">
            <div class="container">
                <div id="books">
                    <div class="row">
                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12" id="space">
                            <div class="col-lg-12">
                                <script async src="//pagead2.googlesyndication.com/pagead/js/adsbygoogle.js"></script>
                                <!-- Bloque1 -->
                                <ins class="adsbygoogle"
                                     style="display:block"
                                     data-ad-client="ca-pub-6359162192439396"
                                     data-ad-slot="7042996067"
                                     data-ad-format="auto"></ins>
                                <script>
                                    (adsbygoogle = window.adsbygoogle || []).push({});
                                </script>
                            </div>
                            <div class="row">
                                <section>
                                    {% if HojadevidaP != null %}
                                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12" id="here">
                                            <div class="row">
                                                <div class="col-xs-12 col-sm-6 col-md-6 col-lg-6">
                                                    <ul>
                                                        <li>Ciudad de recidencia: <span class="">{{HojadevidaP[0].idHojadevida.ciudadresidencia}}</span><hr></li>
                                                        <li>Edad: <span class="">{{aux}} AÑOS</span></li>
                                                    </ul>
                                                </div>
                                                <div class="col-xs-12 col-sm-6 col-md-6 col-lg-6">
                                                    {%set contador=0%}
                                                    {%set aux=false%}
                                                    {% for entity in entities %}
                                                        {%if contador==0%}
                                                            {% if entity.image == ''%}
                                                                {%set aux=true%}
                                                                <img class="featurette-image img-responsive center-block" data-src="{{ asset('upload/default/default.png')}}" alt="Image " src="{{ asset('upload/default/default.png')}}" data-holder-rendered="true">
                                                            {%else%}
                                                                {%set aux=true%}
                                                                <img class="featurette-image img-responsive center-block" data-src="{{web_image('upload/Photos/' ~ entity.id ~'/' ~ entity.image).resize(500,500)}}" alt="Image " src="{{web_image('upload/Photos/' ~ entity.id ~'/' ~ entity.image).resize(500,500)}}" data-holder-rendered="true">
                                                            {%endif%}
                                                        {%endif%}
                                                        {% set contador=contador+1%}
                                                    {%endfor%}
                                                    {%if not aux %}
                                                        <img class="" alt="{{HojadevidaP[0].idHojadevida.id}}" src="{{web_image("/bundles/adminmyaccount/images/no-picture.png").resize(500,500)}}" data-holder-rendered="true">
                                                    {%endif%}
                                                </div>
                                            </div>
                                        </div>
                                    </section>
                                    <section>
                                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12" id="here">
                                            <div class="row">
                                                <div class="col-xs-12 col-sm-6 col-md-6 col-lg-6">
                                                    {%set contador=0%}
                                                    {%set aux=false%}
                                                    {% for entity in entities %}
                                                        {%if contador==1%}
                                                            {% if entity.image == ''%}
                                                                {%set aux=true%}
                                                                <img class="featurette-image img-responsive center-block" data-src="{{ asset('upload/default/default.png')}}" alt="Image " src="{{ asset('upload/default/default.png')}}" data-holder-rendered="true">
                                                            {%else%}
                                                                {%set aux=true%}
                                                                <img class="featurette-image img-responsive center-block" data-src="{{web_image('upload/Photos/' ~ entity.id ~'/' ~ entity.image).resize(500,500)}}" alt="Image " src="{{web_image('upload/Photos/' ~ entity.id ~'/' ~ entity.image).resize(500,500)}}" data-holder-rendered="true">
                                                            {%endif%}
                                                        {%endif%}
                                                        {% set contador=contador+1%}
                                                    {%endfor%}
                                                    {%if not aux %}
                                                        <img class="" alt="{{HojadevidaP[0].idHojadevida.id}}" src="{{web_image("/bundles/adminmyaccount/images/no-picture.png").resize(500,500)}}" data-holder-rendered="true">
                                                    {%endif%}
                                                </div>
                                                <div class="col-xs-12 col-sm-6 col-md-6 col-lg-6">
                                                    <ul>
                                                        <li>Estatura: <span class="">{{HojadevidaP[0].idHojadevida.estatura}} Cm</span><hr></li>
                                                        <li>Color de piel: <span class="">{{HojadevidaP[0].idHojadevida.piel}}</span></li>
                                                    </ul>
                                                </div>
                                            </div>
                                        </div>
                                    </section>
                                    <section>
                                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12" id="here">
                                            <div class="row">
                                                <div class="col-xs-12 col-sm-6 col-md-6 col-lg-6">
                                                    <ul>
                                                        <li>Color de ojos: <span class="">{{HojadevidaP[0].idHojadevida.ojos}}</span><hr></li>
                                                        <li>Cabello: <span class="a">{{HojadevidaP[0].idHojadevida.pelo}}</span></li>
                                                    </ul>
                                                </div>
                                                <div class="col-xs-12 col-sm-6 col-md-6 col-lg-6">
                                                    {%set contador=0%}
                                                    {%set aux=false%}
                                                    {% for entity in entities %}
                                                        {%if contador==2%}
                                                            {% if entity.image == ''%}
                                                                {%set aux=true%}
                                                                <img class="featurette-image img-responsive center-block" data-src="{{ asset('upload/default/default.png')}}" alt="Image " src="{{ asset('upload/default/default.png')}}" data-holder-rendered="true">
                                                            {%else%}
                                                                {%set aux=true%}
                                                                <img class="featurette-image img-responsive center-block" data-src="{{web_image('upload/Photos/' ~ entity.id ~'/' ~ entity.image).resize(500,500)}}" alt="Image " src="{{web_image('upload/Photos/' ~ entity.id ~'/' ~ entity.image).resize(500,500)}}" data-holder-rendered="true">
                                                            {%endif%}
                                                        {%endif%}
                                                        {% set contador=contador+1%}
                                                    {%endfor%}
                                                    {%if not aux %}
                                                        <img class="" alt="{{HojadevidaP[0].idHojadevida.id}}" src="{{web_image("/bundles/adminmyaccount/images/no-picture.png").resize(500,500)}}" data-holder-rendered="true">
                                                    {%endif%}
                                                </div>
                                            </div>
                                        </div>
                                    </section>
                                    <section>
                                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12" id="here">
                                            <div class="row">
                                                <div class="col-xs-12 col-sm-6 col-md-6 col-lg-6">
                                                    {%set contador=0%}
                                                    {%set aux=false%}
                                                    {% for entity in entities %}
                                                        {%if contador==3%}
                                                            {% if entity.image == ''%}
                                                                {%set aux=true%}
                                                                <img class="featurette-image img-responsive center-block" data-src="{{ asset('upload/default/default.png')}}" alt="Image " src="{{ asset('upload/default/default.png')}}" data-holder-rendered="true">
                                                            {%else%}
                                                                {%set aux=true%}
                                                                <img class="featurette-image img-responsive center-block" data-src="{{web_image('upload/Photos/' ~ entity.id ~'/' ~ entity.image).resize(500,500)}}" alt="Image " src="{{web_image('upload/Photos/' ~ entity.id ~'/' ~ entity.image).resize(500,500)}}" data-holder-rendered="true">
                                                            {%endif%}
                                                        {%endif%}
                                                        {% set contador=contador+1%}
                                                    {%endfor%}
                                                    {%if not aux %}
                                                        <img class="" alt="{{HojadevidaP[0].idHojadevida.id}}" src="{{web_image("/bundles/adminmyaccount/images/no-picture.png").resize(500,500)}}" data-holder-rendered="true">
                                                    {%endif%}
                                                </div>
                                                <div class="col-xs-12 col-sm-6 col-md-6 col-lg-6">
                                                    <ul>
                                                        <li>Talla de camisa: <span>{{HojadevidaP[0].idHojadevida.tallaCamisa}}</span><hr></li>
                                                        <li>Talla de pantalon: <span>{{HojadevidaP[0].idHojadevida.tallaPantalon}}</span></li>
                                                    </ul>
                                                </div>
                                            </div>
                                        </div>
                                    </section>
                                    <section>
                                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12" id="here">
                                            <div class="row">
                                                <div class="col-xs-12 col-sm-6 col-md-6 col-lg-6">
                                                    <ul>
                                                        <li>Calzado: <span>{{HojadevidaP[0].idHojadevida.tallaZapato}}</span><hr></li>
                                                        <li>Peso: <span>{{HojadevidaP[0].idHojadevida.peso}} KG</span></li>
                                                    </ul>
                                                </div>
                                                <div class="col-xs-12 col-sm-6 col-md-6 col-lg-6">
                                                    {%set contador=0%}
                                                    {%set aux=false%}
                                                    {% for entity in entities %}
                                                        {%if contador==4%}
                                                            {% if entity.image == ''%}
                                                                {%set aux=true%}
                                                                <img class="featurette-image img-responsive center-block" data-src="{{web_image('upload/default/default.png').resize(100,100)}}" alt="Image " src="{{ asset('upload/default/default.png')}}" data-holder-rendered="true">
                                                            {%else%}
                                                                {%set aux=true%}
                                                                <img class="featurette-image img-responsive center-block" data-src="{{web_image('upload/Photos/' ~ entity.id ~'/' ~ entity.image).resize(500,500)}}" alt="Image " src="{{web_image('upload/Photos/' ~ entity.id ~'/' ~ entity.image).resize(500,500)}}" data-holder-rendered="true">
                                                            {%endif%}
                                                        {%endif%}
                                                        {% set contador=contador+1%}
                                                    {%endfor%}
                                                    {%if not aux %}
                                                        <img class="" alt="{{HojadevidaP[0].idHojadevida.id}}" src="{{web_image("/bundles/adminmyaccount/images/no-picture.png").resize(500,500)}}" data-holder-rendered="true">
                                                    {%endif%}

                                                </div>
                                            </div>
                                        </div>
                                    </section>

                                    <section>
                                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12" id="here">
                                            <div class="row">
                                                <div class="col-xs-12 col-sm-6 col-md-6 col-lg-6">
                                                    {%set contador=0%}
                                                    {%set aux=false%}
                                                    {% for entity in entities %}
                                                        {%if contador==5%}
                                                            {% if entity.image == ''%}
                                                                {%set aux=true%}
                                                                <img class="featurette-image img-responsive center-block" data-src="{{ web_image('upload/default/default.png').resize(100,100)}}" alt="Image " src="{{ asset('upload/default/default.png')}}" data-holder-rendered="true">
                                                            {%else%}
                                                                {%set aux=true%}
                                                                <img class="featurette-image img-responsive center-block" data-src="{{web_image('upload/Photos/' ~ entity.id ~'/' ~ entity.image).resize(500,500)}}" alt="Image " src="{{web_image('upload/Photos/' ~ entity.id ~'/' ~ entity.image).resize(500,500)}}" data-holder-rendered="true">
                                                            {%endif%}
                                                        {%endif%}
                                                        {% set contador=contador+1%}
                                                    {%endfor%}
                                                    {%if not aux %}
                                                        <img class="" alt="{{HojadevidaP[0].idHojadevida.id}}" src="{{web_image("/bundles/adminmyaccount/images/no-picture.png").resize(500,500)}}" data-holder-rendered="true">
                                                    {%endif%}
                                                </div>
                                                <div class="col-xs-12 col-sm-6 col-md-6 col-lg-6">
                                                    <ul id="experiencia">
                                                        <li>Sobre mí:
                                                            <span>
                                                                <hr>
                                                                {{HojadevidaP[0].idHojadevida.experienciaTv}}
                                                            </span>
                                                        </li>
                                                    </ul>
                                                </div>
                                            </div>
                                        </div>
                                    </section>
                                {%else%}
                                    <section>
                                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12" id="here">
                                            <center><h1><small>No existe un book en esta credencial</small></h1></center>
                                          
                                        </div>
                                    </section>
                                {%endif%}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
        <hr>
        <script type="text/javascript" src="{{asset('/bundles/adminmyaccount/js/book/jquery-2.1.4.min.js')}}" charset="UTF-8"></script>
        <script type="text/javascript" src="{{asset('/bundles/adminmyaccount/js/book/bootstrap.min.js')}}"></script>
        <script>
            (function (i, s, o, g, r, a, m) {
                i['GoogleAnalyticsObject'] = r;
                i[r] = i[r] || function () {
                    (i[r].q = i[r].q || []).push(arguments)
                }, i[r].l = 1 * new Date();
                a = s.createElement(o),
                        m = s.getElementsByTagName(o)[0];
                a.async = 1;
                a.src = g;
                m.parentNode.insertBefore(a, m)
            })(window, document, 'script', 'https://www.google-analytics.com/analytics.js', 'ga');

            ga('create', 'UA-79661589-1', 'auto');
            ga('send', 'pageview');

        </script>
    </body>
</html>




